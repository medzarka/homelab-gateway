# ==============================================================================
# TRAEFIK DYNAMIC FILE PROVIDER (External & Non-Docker Upstreams)
# ==============================================================================
# NOTE: All Homelab Docker containers (Homepage, OnlyOffice, SearXNG, Ollama,
# Speaches, Firecrawl, Sandbox, Knowledge-MCP, Seafile, Immich, etc.) define
# their routing rules directly via container labels in their docker-compose.yaml.
#
# Use this file ONLY for manual external upstream hosts (e.g. Proxmox, bare-metal
# hardware, or non-containerized external Tailscale nodes).
# ==============================================================================

http:
  routers: {}
  services: {}
